name: Follows
actions:

  - operationId: createFollow
    summary: Create a follow for a user
    method: POST
    path: /follows
    parameters:
    - name: user_id
      type: string
      description: GiveyTag or ID of a User
